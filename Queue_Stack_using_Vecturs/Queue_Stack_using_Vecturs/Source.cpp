#include<iostream>
#include "queue.h"
#include "vectur.h"
#include <vector>
using namespace std;


int main() {
	MyQueue<int> hello(90);
	hello.push(80);
	hello.push(60);
	hello.push(50);
	hello.push(40);
	hello.push(30);
	hello.push(20);
	hello.push(10);
	hello.push(5);
	hello.pop();
	hello.pop();
	hello.pop();
	hello.pop();
	hello.pop();
	hello.pop();
	hello.pop();
	hello.pop();
	hello.pop();
	hello.pop();
	vector<int> sup;
	sup.push_back(90);
	

	
}